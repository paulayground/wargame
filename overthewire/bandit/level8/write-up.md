1. 이전 레벨에서 획득한 pw로 ssh를 통한 host의 2220으로 접속
2. data.txt에 중복되는 내용들로 섞여있어, 문제에서 제시한 것과 같이 유일한 값인 flag를 찾기위해 `sort data.txt | uniq -u`와 같이 data.txt를 정렬하고 중복된 부분을 제거하여 flag를 획득한다.
uniq의 경우 분산된 중복을 찾지 못해 sort로 정렬이 필요하며, `-u`옵션을 통해 중복라인이 없는 것만 출력한다.